<UserControl x:Class="CryptoApp.Views.CryptoDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CryptoApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Button Content="Back-" Command="{Binding BackCommand}" Width="30"/>
            <TextBlock Text="{Binding CryptoCurrency.Name}" 
                       FontSize="20" FontWeight="Bold"
                       VerticalAlignment="Center" Margin="10,0,0,0"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Vertical">
            <Image Source="{Binding CryptoCurrency.Image.Large}" Height="100"/>
            <TextBlock Text="{Binding CryptoCurrency.Symbol}" FontSize="16"/>
            <TextBlock Text="Current price in USD:" FontWeight="Bold"/>
            <TextBlock Text="{Binding CryptoCurrency.MarketData.CurrentPrice[usd]}"/>
            <TextBlock Text="{Binding CryptoCurrencyDescription}" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>

</UserControl>
